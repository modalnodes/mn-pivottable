import { SetupDirective } from './setup.directive';

describe('SetupDirective', () => {
  it('should create an instance', () => {
    const directive = new SetupDirective();
    expect(directive).toBeTruthy();
  });
});
